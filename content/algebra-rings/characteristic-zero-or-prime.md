---
title: "Characteristic of an integral domain is 0 or prime"
description: "An integral domain cannot have composite positive characteristic."
---

**Characteristic of an integral domain is 0 or prime**: If $D$ is an integral domain, then $\operatorname{char}(D)$ is either $0$ or a prime number $p$.

The proof analyzes the kernel of the canonical map $\mathbb Z\to D$ using {{</* knowl id="kernel-is-ideal" text="the kernel–ideal property" */>}}, and thereby determines the {{</* knowl id="characteristic" text="characteristic" */>}} of an {{</* knowl id="integral-domain" text="integral domain" */>}}. This dichotomy is the starting point for constructing the smallest subfield inside any {{</* knowl id="field" text="field" */>}} (see the corollary on prime subfields).